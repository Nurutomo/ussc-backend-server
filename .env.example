# Copy this file to .env and fill in real values. .env is gitignored.

PORT=3000

# Option A: full connection string (takes priority over the individual vars below)
# DB_URL=mysql://user:password@host:3306/database_name

# Option B: individual connection settings
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=marker
